#include <iostream>
#include <string>
#include <cmath>
#include <fstream>

using namespace std;

struct Team{
	//int num, average, match_num, alliance_score, match_penalties, defense, score;
	int num, average, match_num;
	string name; 
	Team(){}
};

int main(){
	Team test;	
	return 0;
}
