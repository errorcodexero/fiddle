#include <iostream>
#include <time.h>
#include <cstdlib>
using namespace std;

struct point {
	int x, y;
};

const int X_LENGTH = 20, Y_LENTGH = 20;

int wall[] ={0,1};

int main() {
	srand(time(0));
	return 0;
}
